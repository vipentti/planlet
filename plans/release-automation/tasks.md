# Tasks: Release Automation

- [x] T1 Add `CHANGELOG.md` in Keep a Changelog 1.1.0 format with empty `Unreleased`, a 0.1.0 section reviewed against repository changes and user-visible behavior, and compare links
- [x] T2 Package and format `CHANGELOG.md`, and assert its presence in the packed-artifact integration test
- [x] T3 Add dependency-free `scripts/changelog.mjs` extracting one dated version and rejecting missing, empty, or `Unreleased` sections
- [x] T4 Add focused subprocess coverage against the repository changelog and an isolated temp file for known, absent, empty, and `Unreleased` sections
- [x] T5 Add `.github/workflows/release.yml` (tag `v*` only) pinned to the same reviewed `actions/checkout` and `actions/setup-node` SHAs as `ci.yml`, with version/main-ancestry guards, full verify suite, `.agents`/`.claude` skill parity, clean-tree check, changelog notes via `scripts/changelog.mjs`, `npm pack` then publish-or-verify using `package.json` name `@vipentti/planlet`, provenance/`--access public`, release create/update, and same-tag concurrency without cancelling in-flight runs
- [x] T6 Document prepublication source-only install and changelog upkeep in `README.md`, and point release guidance at this planlet
- [x] T7 Review full history, refs intended for exposure, commit metadata, and stored objects for secrets/credentials, licensing or third-party provenance, and personal data; resolve every finding and record captain sign-off before visibility changes
- [x] T8 Before packing, set the `[0.1.0]` changelog header date to the intended publish day on `main` if it still differs; then from captain-approved clean `origin/main` SHA, reproduce and record exact 0.1.0 package metadata, file list, integrity, shasum, and SHA-256; inspect the tarball; then publish only that approved `.tgz` and verify registry state
- [ ] T9 On `main`, prepare `@vipentti/planlet@0.1.1`, push its reviewed commit before its exact tag, run the automated `release.yml` workflow, and record workflow conclusion, npm provenance/integrity, installed package, rendered release body, and safe rerun evidence
- [x] T10 Immediately before bootstrap, verify npm name availability, authenticated account and 2FA/auth readiness, intended owner identity, package ownership state, and public-access capability; record failure/retry response without claiming the name
- [x] T11 After T7 and captain public-release authorization, make `vipentti/planlet` public and independently verify anonymous repository and full-history visibility
- [x] T12 After verified T8 publication and while release workflow is absent from default branch, create `v0.1.0` at exact approved SHA and create matching GitHub release from the dated 0.1.0 changelog notes at that SHA
- [x] T13 After bootstrap package ownership is verified, configure npm trusted publisher for package `@vipentti/planlet` on GitHub repository `vipentti/planlet` with workflow `.github/workflows/release.yml`, then independently verify configuration
- [x] T14 Land Slice B (`release.yml` plus T18 docs) on `main` only after T12-T13, verify fast-forward source and green main CI, and confirm no release tag was pushed during merge
- [x] T15 Record captain release-governance choice: accept current strict CI, signed-commit, linear-history, no-force-push/no-deletion, administrator-not-enforced `main` protection, or implement and verify additional tag controls and/or a protected GitHub environment before T13
- [x] T16 Record captain bootstrap authorization naming npm owner/account, exact clean source SHA, reviewed artifact identity/hash, and permission for irreversible 0.1.0 publication
- [x] T17 Reconcile Slice B onto current `main` as source of truth: keep main planlet evidence and completed bootstrap tasks, add adapted `release.yml` (not stale unscoped Slice A content), align docs without rewriting write-once evidence, and pass focused, full-suite, generated-skill parity, and packaging checks
- [x] T18 Document post-bootstrap install for `@vipentti/planlet`, publication gates, and the common tag-triggered `release.yml` flow in `README.md`; point `AGENTS.md` at those owner documents without duplicating procedure; on planlet completion retarget any README link from `plans/release-automation/` to the completed archive path

## Verification Evidence

- 2026-07-31 — Full-history audit found no unresolved credentials, personal-data, binary or large-object, or licensing/provenance findings; captain approved the audit while the repository remained private.
- 2026-08-03 — npm preflight (T10): registry returned 404 for `planlet`; search empty; authenticated as `vipentti` with email verified and tfa mode `auth-and-writes`; `npm access list packages vipentti` empty (no prior ownership); name not claimed.
- 2026-08-03 — Captain accepted current `main` protection for release governance (T15): strict required CI across six OS/Node jobs, signed commits, linear history, no force-push/deletion, administrator enforcement disabled; no additional tag controls or protected release environment before T13.
- 2026-08-03 — Captain authorized public release of `vipentti/planlet` after T7 audit (full history exposure).
- 2026-08-03 — Captain authorized bootstrap (T16): npm owner/account `vipentti`, package name `planlet`, clean source SHA `36d2175eac836fe9fcdc6e40977a7a247bdb66a1`, irreversible 0.1.0 publication permitted.
- 2026-08-03 — Repository `vipentti/planlet` set to public (T11); anonymous API returned public visibility, bootstrap SHA and earlier history commit reachable without auth.
- 2026-08-03 — Reviewed bootstrap artifact from SHA `36d2175eac836fe9fcdc6e40977a7a247bdb66a1`: `planlet@0.1.0` file `planlet-0.1.0.tgz`; integrity `sha512-dq1uPZzoVFSPylITYDBrnja8l4PEVqkam8cP79EaSug3I+Gu8P326b0HqfjTyPTtn6DlpxO4Dh824u4vDNZhEA==`; shasum `224f4eadba35996c367d4134239de0f6c0156c90`; SHA-256 `bcab3dc8af55227fcfab9daad9528834308506b250e33c9da5ff43fbcb45cdca`; 16 entries (CHANGELOG/LICENSE/README/dist/skills); inspect found no unexpected files or credential material. Captain-authorized for publish; registry publish pending OTP.
- 2026-08-03 — First `npm publish` of that unscoped artifact failed with E403: package name too similar to existing `blanket`; registry suggested `@vipentti/planlet`. No package was created. Prior artifact abandoned; do not retry unscoped publish.
- 2026-08-03 — Captain chose scoped package name `@vipentti/planlet` for bootstrap; CLI binary remains `planlet`. New clean `main` SHA and reviewed artifact required before republish.
- 2026-08-03 — Reviewed scoped bootstrap artifact from SHA `53eb188e753503564206abb5ca6118d5aa201e5f`: `@vipentti/planlet@0.1.0` file `vipentti-planlet-0.1.0.tgz`; integrity `sha512-JvtLGRJbmXlmfWGLnI+4Byr5G/ly4ncAd6h4GISweNkrOQv8AxER3WpjDHN4J3XPIQ1uHTMqk2Epm82rr/FCfg==`; shasum `e390fa4ef724f439014968ff4681c8c7cb1a9fa0`; SHA-256 `d6608ab4904998757dfdb23256c054d0c16fad26fe57fddca9c2f1d359528de1`; 16 entries; bin `planlet`; inspect found no unexpected files or credential material. Captain-authorized for publish with `--access public` + passkey.
- 2026-08-03 — Registry verified `@vipentti/planlet@0.1.0` with dist.integrity matching approved artifact `sha512-JvtLGRJbmXlmfWGLnI+4Byr5G/ly4ncAd6h4GISweNkrOQv8AxER3WpjDHN4J3XPIQ1uHTMqk2Epm82rr/FCfg==` (T8 publish complete).
- 2026-08-03 — Created annotated tag `v0.1.0` at `53eb188e753503564206abb5ca6118d5aa201e5f` and GitHub release from dated 0.1.0 changelog notes while `release.yml` absent from default branch (T12).
- 2026-08-03 — GitHub repository homepage set to `https://www.npmjs.com/package/@vipentti/planlet`.
- 2026-08-03 — Captain configured npm trusted publisher for package `@vipentti/planlet` on GitHub repository `vipentti/planlet` with workflow filename `release.yml`, no GitHub environment, publishing access requiring 2FA and disallowing bypass-2FA tokens (T13).
- 2026-08-03 — Squash-merged Slice B to `main` as `0c3d63985989e252d11fcdcf64b51c71c4e27fb3` (`release.yml` present); only existing release tag remained `v0.1.0` (no new tag during merge) (T14).
